# 生产环境环境变量（Vercel 部署时配置）
# 后端 API 地址（Render 部署后的地址，不要加末尾斜杠）
VITE_API_BASE_URL=https://your-app-name.onrender.com/api/v1

# WebSocket 地址（可选，不填则从 API 地址推导）
# VITE_WS_BASE_URL=wss://your-app-name.onrender.com
